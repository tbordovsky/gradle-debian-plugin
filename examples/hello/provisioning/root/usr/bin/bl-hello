#!/bin/bash

# export everything from profile
set -o allexport
. /etc/profile.d/blacklocus

readonly app_name=bl-hello

cd /opt/blacklocus/$app_name
./bin/$app_name server >> /var/log/blacklocus/$app_name.log 2>&1
